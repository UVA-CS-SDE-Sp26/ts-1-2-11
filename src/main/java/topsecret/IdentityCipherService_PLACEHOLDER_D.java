package topsecret;

public class IdentityCipherService_PLACEHOLDER_D implements CipherService {
    @Override
    public String decipher(String cipherText, String keyPath) {
        return cipherText;
    }
}
